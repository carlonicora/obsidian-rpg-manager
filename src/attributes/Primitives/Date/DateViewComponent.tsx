import React from "react";
import { Attribute } from "src/interfaces/Attribute";
import { Element } from "src/interfaces/Element";

export function DateViewComponent({
	element,
	attribute,
}: {
	element: Element;
	attribute: Attribute;
}): React.ReactElement {
	return <></>;
}
