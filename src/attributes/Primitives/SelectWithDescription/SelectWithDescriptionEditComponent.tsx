import React from "react";
import { Attribute } from "src/interfaces/Attribute";
import { Element } from "src/interfaces/Element";

export function SelectWithDescriptionEditComponent({
	element,
	attribute,
}: {
	element: Element;
	attribute: Attribute;
}): React.ReactElement {
	return <></>;
}
