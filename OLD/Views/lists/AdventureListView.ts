export class AdventureListView {
	/*
	async render(
		data: AdventureListInterface
	): Promise<void>
	{
		this.dv.span("## Adventures");

		this.dv.table(["", "Adventure", "Synopsis"],
			data.elements
				.map(adventure => [
					adventure.id,
					adventure.link,
					adventure.synopsis,
				])
		);

		this.spacer();
	}

	 */
}
