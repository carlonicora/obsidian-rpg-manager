export class FactionListView {
	/*
	async render(
		data: FactionListInterface
	): Promise<void>
	{
		this.dv.span("## Factions");

		this.dv.table(["", "Faction", "Synopsis"],
			data.elements
				.map(clue => [
					clue.image,
					clue.link,
					clue.synopsis,
				])
		);

		this.spacer();
	}

	 */
}
